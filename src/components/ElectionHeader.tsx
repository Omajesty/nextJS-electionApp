export function ElectionHeader() {
  return (
    <header className="mb-8 text-center">
      <p className="mb-2 text-sm font-semibold uppercase tracking-[0.35em] text-primary">
        Africa Plan Foundation Voting System
      </p>
      <h1 className="text-3xl font-bold text-primary sm:text-4xl">
        HOH Election
      </h1>
      <p className="mx-auto mt-3 max-w-2xl text-sm text-base-content/70 sm:text-base">
        Cast your vote with confidence, we are not APC.
      </p>
    </header>
  );
}
